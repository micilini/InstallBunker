# InstallBunker
InstallBunker is a self-contained Windows installer builder. Package your desktop apps into professional Setup and Uninstaller executables through a clean wizard UI — no external tools or .NET SDK required on the build machine.
